from django.urls import path

# Listings API router mapping view stubs:
# path("", ListingListCreateView.as_view(), name="listings"),
# path("<int:pk>", ListingDetailView.as_view(), name="listing-detail"),
urlpatterns = []
