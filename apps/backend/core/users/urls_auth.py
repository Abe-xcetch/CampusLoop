from django.urls import path

# Stubs for Firebase token actions if needed. 
# Authentication is intercepted at request-level using FirebaseAuthentication,
# so explicit authentication endpoints are mostly managed on the frontend.
urlpatterns = []
