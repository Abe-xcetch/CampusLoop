from django.urls import path

# Admin actions routing:
# path("listings/pending", PendingListingsView.as_view(), name="admin-pending-listings"),
# path("listings/<int:pk>/approve", ApproveListingView.as_view(), name="admin-approve-listing"),
# path("users/<str:user_id>/ban", BanUserView.as_view(), name="admin-ban-user"),
# path("reports", ListReportsView.as_view(), name="admin-list-reports"),
# path("reports/<int:pk>/resolve", ResolveReportView.as_view(), name="admin-resolve-report"),
urlpatterns = []
