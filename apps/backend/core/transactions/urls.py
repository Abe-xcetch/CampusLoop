from django.urls import path

# Transactions API router mapping:
# path("", TransactionCreateListView.as_view(), name="transactions"),
# path("<int:pk>/confirm", ConfirmTransactionView.as_view(), name="transaction-confirm"),
# path("<int:pk>/complete", CompleteTransactionView.as_view(), name="transaction-complete"),
urlpatterns = []
