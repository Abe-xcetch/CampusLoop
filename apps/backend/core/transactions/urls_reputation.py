from django.urls import path

# Reputation score and reviews API routing:
# path("reviews", CreateReviewView.as_view(), name="create-review"),
# path("user/<str:user_id>", UserReviewsListView.as_view(), name="user-reviews"),
urlpatterns = []
